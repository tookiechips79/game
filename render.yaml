services:
  - type: web
    name: gamebird-app
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run dev:full
    envVars:
      - key: NODE_ENV
        value: production
